tupla = (1, "string", True)
print(tupla)
print(tupla[0])
print(tupla[0:1])
#No podemos mutar la tupla, es decir, no podemos agregar ni eliminar elementos
#Solo usaremos tuplas cuando necesitemos tener valores totalmente fijos
